# Credits to https://github.com/BamPeers/rust-ci-github-actions-workflow/tree/main
on:
  pull_request:
    paths: ["src/api/**"]
  push:
    paths: ["src/api/**"]
    branches:
      - main

name: Check and Lint

jobs:
  check:
    name: 🔧 Check
    runs-on: ubuntu-latest
    working-directory: src/api
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - uses: actions-rs/cargo@v1
        with:
          command: check

  fmt:
    name: 📦 Rustfmt
    runs-on: ubuntu-latest
    working-directory: src/api
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          profile: minimal
          toolchain: stable
          override: true
      - run: rustup component add rustfmt
      - uses: actions-rs/cargo@v1
        with:
          command: fmt
          args: --all -- --check

  clippy:
    name: 📦 Clippy
    runs-on: ubuntu-latest
    working-directory: src/api
    steps:
      - uses: actions/checkout@v2
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          components: clippy
          override: true
      - uses: actions-rs/clippy-check@v1
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          args: --all-features
          name: Clippy Output
